import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tripoint.tripagos',
  appName: 'TriPagos',
  webDir: 'dist/tripagos/browser'
};

export default config;
